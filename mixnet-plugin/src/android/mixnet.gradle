repositories{
    jcenter()
    flatDir{
        dirs 'libs'
    }
}

dependencies {
    compileOnly files('libs/mixnet.aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
